name: Deploy to Vercel

on:
  push:
    branches: [main]
    paths:
      - 'dashboard/frontend/**'
  workflow_dispatch:

jobs:
  deploy-frontend:
    name: Deploy Frontend to Vercel
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: dashboard/frontend/package-lock.json

      - name: Generate package-lock.json
        run: npm install --package-lock-only
        working-directory: dashboard/frontend

      - name: Install dependencies
        run: npm ci
        working-directory: dashboard/frontend

      - name: Run tests
        run: npm test -- --watchAll=false --ci --coverage=false
        working-directory: dashboard/frontend
        continue-on-error: true

      - name: Build
        run: npm run build
        working-directory: dashboard/frontend
        env:
          REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        if: github.ref == 'refs/heads/main'
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
          working-directory: ./dashboard/frontend

      - name: Preview Deployment URL
        if: github.event_name == 'pull_request'
        run: |
          echo "Preview deployed to: ${{ steps.deploy.outputs.url }}"

  # Placeholder for backend deployment (configure when ready)
  # deploy-backend:
  #   name: Deploy Backend (configure when ready)
  #   runs-on: ubuntu-latest
  #   needs: deploy-frontend
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #     - name: Deploy to Render/Railway/Fly.io
  #       run: |
  #         echo "Configure your backend deployment here"
  #         echo "Options: Render, Railway, Fly.io, AWS ECS, Google Cloud Run, etc."